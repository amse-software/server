import { RequestHandler } from "express";

export function getParticipantsByGroup(): RequestHandler {
  return (req, res, next) => {};
}

export function createParticipant(): RequestHandler {
  return (req, res, next) => {};
}

export function getAllParticipants(): RequestHandler {
  return (req, res, next) => {};
}

export function getParticipant(): RequestHandler {
  return (req, res, next) => {};
}

export function updateParticipant(): RequestHandler {
  return (req, res, next) => {};
}

export function deleteParticipant(): RequestHandler {
  return (req, res, next) => {};
}
