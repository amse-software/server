import { RequestHandler } from "express";

export function createGroup(): RequestHandler {
  return (req, res, next) => {};
}

export function getAllGroups(): RequestHandler {
  return (req, res, next) => {};
}

export function getGroup(): RequestHandler {
  return (req, res, next) => {};
}

export function updateGroup(): RequestHandler {
  return (req, res, next) => {};
}

export function deleteGroup(): RequestHandler {
  return (req, res, next) => {};
}
